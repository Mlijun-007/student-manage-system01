# 教育培训管理系统需求开发文档

## 一、文档概述

### （一）文档目的

本文档旨在明确 “学海” 培训中心管理系统的功能需求、数据需求、接口需求等核心内容，为参与项目开发的开发人员提供统一的参考标准，确保开发工作有序推进，最终实现满足培训中心业务需求的轻量级内部管理系统。

### （二）项目背景

“学海” 培训中心是一家小型线下教育培训机构，主营 K12 课外辅导和成人职业技能培训。此前，机构依赖 Excel 表格管理学员信息、课程及班级，存在数据分散、版本混乱、查询统计低效、运营情况无法直观展示等问题。随着业务规模扩大，亟需一套集中化、规范化的内部管理系统，提升日常工作效率。本项目为大学生小组作业，采用 Spring Boot、Vue、MyBatis、MySQL 技术栈进行开发。

### （三）适用范围

本文档适用于参与该项目的所有前后端开发人员，作为开发过程中功能实现、数据设计、接口对接的核心依据。

## 二、总体需求

### （一）核心目标

实现学员、课程、班级信息的集中化、规范化管理，提供便捷的查询、编辑、删除等操作，通过首页仪表盘直观展示机构运营核心数据，满足培训中心日常管理需求。

### （二）功能架构

系统主要包含四大核心功能模块及首页仪表盘，具体架构如下：
学员管理模块：实现学员信息的录入、查询、编辑、删除功能；
课程管理模块：完成课程信息的维护、列表展示、编辑及删除操作；
班级管理模块：支持班级创建、列表展示及详情查看，实现班级与学员、课程的关联管理；
首页仪表盘模块：统计并展示核心运营数据，便于快速掌握机构概况。

### （三）性能要求

响应速度：页面加载及操作响应时间不超过 3 秒；
数据准确性：数据的新增、修改、删除操作需保证数据一致性，无冗余或错误数据；
兼容性：支持主流浏览器（Chrome、Firefox、Edge）的最新版本。
数据操作需进行合法性校验，防止非法数据录入；
关键操作（如删除）需提供二次确认机制，避免误操作。

## 三、详细功能需求

### （一）学员管理模块

1. 学员信息录入
字段说明：
学员 ID：系统自动生成（自增或 UUID），不可修改；
姓名：必填项，无长度限制；
性别：必填项，可选值为男、女、其他；
联系电话：必填项，需符合手机号格式规范；
所属班级：必填项，从已创建的班级中下拉选择；
报名时间：默认填充当前日期，支持手动修改；
备注：可选填项，无长度限制。
功能要求：提供学员信息录入入口，对必填项进行合法性校验，校验失败需给出明确提示。

2. 学员查询
查询条件：支持按姓名模糊查询、按所属班级精确查询，可单独使用某一条件或组合查询；
结果展示：查询结果以表格形式展示，包含字段：学员 ID、姓名、性别、联系电话、所属班级、报名时间。

3. 学员编辑与删除
编辑：支持点击学员记录进入编辑页面，可修改除学员 ID 外的所有字段，修改后需进行数据校验；
删除：支持删除学员记录，删除前需弹出二次确认对话框，确认后执行删除操作。

### （二）课程管理模块

1. 课程信息维护
字段说明：
课程 ID：系统自动生成，不可修改；
课程名称：必填项，无长度限制；
课程描述：可选填项，无长度限制；
课时总数：必填项，需为非负整数；
课程价格：必填项，支持整数或小数格式。
功能要求：提供课程新增、编辑入口，对课时总数和课程价格进行数字格式校验，校验失败给出提示。

2. 课程列表展示
展示内容：以表格形式展示所有课程信息，包含字段：课程 ID、课程名称、课时总数、课程价格、课程描述；
功能支持：列表需清晰展示各字段信息，便于快速浏览。

3. 课程删除
删除规则：若课程未被任何班级引用，可执行删除操作；若课程已被班级关联引用，禁止删除并给出提示：“该课程已有班级在使用，无法删除”；
操作流程：点击删除按钮后，系统先校验课程引用状态，再根据校验结果执行相应操作或提示。

### （三）班级管理模块

1. 班级创建
字段说明：
班级 ID：系统自动生成，不可修改；
班级名称：必填项，无长度限制；
关联课程：必选项，从已创建的课程中下拉选择（课程列表从后台动态加载）；
开班时间：必填项，通过日期选择控件选择；
授课老师：必填项，文本输入格式；
班级状态：必填项，可选值为未开班、已开班、已结课。
功能要求：提供班级创建入口，关联课程下拉框需动态加载后台课程数据，开班时间支持日期控件选择。

2. 班级列表展示
展示内容：以表格形式展示所有班级信息，包含字段：班级 ID、班级名称、关联课程、开班时间、授课老师、班级状态；
搜索功能：支持按班级名称进行模糊搜索（可选功能，根据开发进度决定是否实现）。

3. 班级详情
基础信息展示：展示该班级的完整基础信息（班级名称、关联课程、开班时间、授课老师、班级状态等）；
学员列表展示：展示该班级下所有学员信息，包含字段：姓名、性别、联系电话、报名时间；
快捷功能：提供 “新增学员” 快捷入口，点击后跳转至学员录入页面，且所属班级字段默认填入当前班级。

### （四）首页仪表盘模块

1. 数据统计内容
核心统计项：学员总数、课程总数、班级总数、今日新报名学员数；
数据来源：学员总数、课程总数、班级总数为系统内对应数据的累计值；今日新报名学员数为报名时间为当前日期的学员累计值。

2. 展示要求
布局形式：采用数字卡片布局，卡片风格统一，色彩层次清晰，便于区分不同统计项；
视觉效果：界面简洁直观，支持管理员快速浏览核心数据，掌握机构运营概况。

## 四、数据需求

### （一）数据实体及属性

1. 学员实体（Student）
字段名称 数据类型 约束条件 说明
studentId 字符串 / 整数 主键，非空 系统自动生成（自增 / UUID）
name 字符串 非空 学员姓名
gender 字符串 非空 性别（男 / 女 / 其他）
phone 字符串 非空 联系电话（符合手机号格式）
classId 字符串 / 整数 非空 所属班级 ID（关联班级表）
registerTime 日期时间 非空 报名时间（默认当前日期）
remark 字符串 可空 备注信息

2. 课程实体（Course）
字段名称 数据类型 约束条件 说明
courseId 字符串 / 整数 主键，非空 系统自动生成
courseName 字符串 非空 课程名称
description 字符串 可空 课程描述
classHour 整数 非空，大于等于 0 课时总数
price 小数 / 整数 非空，大于等于 0 课程价格

3. 班级实体（Clazz）
字段名称 数据类型 约束条件 说明
classId 字符串 / 整数 主键，非空 系统自动生成
className 字符串 非空 班级名称
courseId 字符串 / 整数 非空 关联课程 ID（关联课程表）
startDate 日期 非空 开班时间
teacher 字符串 非空 授课老师
status 字符串 非空 班级状态（未开班 / 已开班 / 已结课）

### （二）数据关系

班级与课程：多对一关系，一个课程可被多个班级关联引用，一个班级仅关联一个课程；
班级与学员：一对多关系，一个班级可包含多个学员，一个学员仅属于一个班级。

## 五、接口需求

### （一）接口设计原则

接口命名规范：采用 “模块名 - 操作名” 的格式，例如 “student-add”（学员新增）、“course-list”（课程列表查询）；
请求方式：新增、修改操作采用 POST 请求，查询操作采用 GET 请求，删除操作采用 DELETE 请求；
响应格式：统一采用 JSON 格式，包含状态码、提示信息、响应数据三部分，示例如下：

```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {}
}
```

其中，状态码 200 表示操作成功，500 表示服务器错误，400 表示参数错误。

### （二）核心接口列表

- 学员管理接口

| 接口名称 | 请求方式 | 接口功能   | 请求参数 | 响应数据 |
|---------|---------|------|------|-------|
| student-add | POST | 新增学员 | 姓名、性别、电话、班级 ID、报名时间、备注 | 新增学员的完整信息 |
| student-list | GET | 查询学员列表 | 姓名（可选）、班级 ID（可选）| 学员列表数据（分页 / 全量）|
| student-get | GET  | 获取学员详情 | 学员 ID  | 单个学员的完整信息   |
| student-update| POST | 编辑学员 | 学员 ID、姓名、性别、电话、班级 ID、报名时间、备注 | 修改后的学员完整信息   |
| student-delete| DELETE | 删除学员 | 学员 ID | 操作结果提示 |

- 课程管理接口

| 接口名称      | 请求方式 | 接口功能   | 请求参数  | 响应数据       |
|------|---------|-----|-----------|---------|
| course-add   | POST  | 新增课程   | 课程名称、课程描述、课时总数、课程价格   | 新增课程的完整信息    |
| course-list   | GET   | 查询课程列表 | 无 | 课程列表数据    |
| course-get    | GET   | 获取课程详情 | 课程 ID      | 单个课程的完整信息   |
| course-update | POST     | 编辑课程   | 课程 ID、课程名称、课程描述、课时总数、课程价格 | 修改后的课程完整信息  |
| course-delete | DELETE   | 删除课程   | 课程 ID    | 操作结果提示     |

- 班级管理接口

| 接口名称   | 请求方式 | 接口功能   | 请求参数  | 响应数据  |
|-------------|--------|------|--------|---------|
| clazz-add   | POST  | 新增班级   | 班级名称、课程 ID、开班时间、授课老师、班级状态 | 新增班级的完整信息    |
| clazz-list   | GET   | 查询班级列表   | 班级名称（可选）  | 班级列表数据 |
| clazz-get   | GET  | 获取班级详情   | 班级 ID     | 班级基础信息 + 该班级学员列表 |
| clazz-update  | POST | 编辑班级  | 班级 ID、班级名称、课程 ID、开班时间、授课老师、班级状态 | 修改后的班级完整信息 |
| course-options | GET   | 获取课程下拉选项 | 无    | 课程 ID、课程名称列表    |

-  仪表盘接口

| 接口名称 | 请求方式 | 接口功能|  请求参数 | 响应数据
|-|-|-|-|-|
|dashboard-stat |GET| 获取统计数据| 无| 学员总数、课程总数、班级总数、今日新报名学员数

## 六、其他需求

### （一）界面需求

整体风格：简洁、清晰、易用，符合教育培训管理场景的使用习惯；
布局要求：各模块页面布局统一，操作入口明确，减少用户学习成本；
提示信息：所有操作结果（成功、失败、校验错误）需给出友好、明确的提示信息，便于用户操作。

### （二）可扩展性需求

系统设计需考虑一定的可扩展性，便于后续根据需求新增功能（如权限管理、缴费管理等），但当前开发阶段仅需满足上述核心需求。

### （三）开发协作需求

前后端开发人员需严格按照本文档定义的接口规范进行对接，确保数据交互正常；
数据库表结构需根据本文档的数据实体及关系设计，字段名称、数据类型需保持一致；
开发过程中若需修改需求，需经小组讨论确认后，同步更新本文档，确保所有开发人员使用最新版本的需求文档。
